package attacks;

public class AttackParamTransform {

    // TODO
    /*
    php like transformations
    param=12345 =>
    param (null)
        param= (no value)
    param[]= (array)
    param[0] (array)
    input['param'] (array)

    param=1&param[]=2 (different types, duplicate)

    param{} (hash?)
    */
}
