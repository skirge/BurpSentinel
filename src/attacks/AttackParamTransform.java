package attacks;

public class AttackParamTransform {

    // TODO
    /*
    php like transformations
    param=12345 =>
    param (null)
    param= (no value)
    param[]= (array)
    param[0] (array)
    input['param'] (array)

    param=orig&param[]=attackstring (different types, duplicate)

    param{}= (hash? is that possible?)

    for value = value
    param=val&param=ue (HPP)

    */
}
